anthropic>=0.40.0
python-dotenv>=1.0.0
pydantic>=2.6.0
httpx>=0.27.0
numpy>=1.26.0
sentence-transformers>=2.7.0
# torch: cài bản CPU ổn định từ index chính thức của PyTorch (bản 2.14 trên PyPI
# fail load DLL c10.dll trên Windows). Lệnh cài:
#   pip install torch==2.8.0 --index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
# Giao diện người dùng (app.py)
gradio>=5.0.0